ext {
    // Vaertsnavnet appen tager App Links for. Skal vaere det domaene, siden
    // ligger paa, og der skal ligge en matchende assetlinks.json paa
    // https://<vaert>/.well-known/assetlinks.json - se scripts/assetlinks.js.
    // Er det ikke sat op, fejler verifikationen i stilhed og links åbner
    // som hidtil i browseren.
    appLinkHost = "madplan.example"

    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}